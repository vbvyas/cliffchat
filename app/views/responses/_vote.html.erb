<%= form_for(current_user.votes.build(votedresponse_id: (votedresponse ||= @response).id),
            remote: true) do |f| %>
      <div><%= f.hidden_field :votedresponse_id %></div>
      <%= f.submit "Vote", class: "" %>
<% end %>
