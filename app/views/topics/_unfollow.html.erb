<%= form_for(current_user.followerships.find_by_topic_id(@topic),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= link_to "Follow", "#", class: "btn btn-primary", onclick: "$('form').submit()" %>
<% end %>
