<%= form_for(current_user.followerships.build(topic_id: @topic.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :topic_id %></div>
  <%= link_to "Follow", "#", class: "btn btn-primary", onclick: "$('form').submit()" %>
<% end %>
