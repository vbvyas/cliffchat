<ul class="miniposts" id="miniposts" summary="User posts">
  <div id="postcontainer">
    <%= render :partial => 'shared/feed_item', :collection => @feed_items %>
    <%= will_paginate @feed_items %>
    <%= pageless @feed_items.total_pages, '?escape=false' %>
  </div>
</ul>
